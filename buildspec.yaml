version: 0.2

phases:
  install:
    commands:
      - echo "Install phase - nothing to install"

  build:
    commands:
      - echo "Build phase - listing files"
      - ls -al

  post_build:
    commands:
      - echo "Post-build phase completed"

artifacts:
  files:
    - '**/*'
